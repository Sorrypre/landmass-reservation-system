services:
  - type: web
    name: landmass-reservation-system
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node ./backend/app.js
    envVars:
      - key: MDB_URI
        sync: false
      - key: NODE_ENV
        value: production